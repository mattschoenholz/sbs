# Claude Code internal state (session data, worktrees — not skills)
.claude/
!.claude/commands/
!.claude/commands/*.md

# ESPHome secrets and build artifacts — NEVER commit
secrets.yaml
**/secrets.yaml
.esphome/
esphome/.esphome/

# Watch firmware — auto-generated secrets header (source: esphome/secrets.yaml)
watch/src/secrets.h

# PlatformIO build artifacts
watch/.pio/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.env
.venv
venv/

# Node
node_modules/

# macOS
.DS_Store
.AppleDouble

# Large chart data — lives on the Pi, not in this repo
/data/
*.mbtiles
*.gpkg
*.pmtiles

# Logs / temp
*.log
/tmp/
