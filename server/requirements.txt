# SailboatServer Python dependencies
# Install: pip3 install -r requirements.txt
# On Pi: sudo pip3 install -r requirements.txt --break-system-packages

flask>=2.0
flask-cors>=4.0
paramiko>=2.0
# lgpio — installed via apt on Pi: sudo apt install python3-lgpio
