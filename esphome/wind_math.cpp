// wind_math.cpp — translation unit so the header can be compiled standalone
// for the parity test harness. ESPHome includes wind_math.h directly via the
// `esphome.includes:` mechanism; this .cpp is only used by test_wind_math_cpp.
//
// All functions are defined `inline` in the header.

#include "wind_math.h"
